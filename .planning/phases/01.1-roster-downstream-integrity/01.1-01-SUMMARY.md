---
phase: 01.1-roster-downstream-integrity
plan: "01"
subsystem: api
tags: [dashboard, sqlalchemy, shift-assignment, payroll]

# Dependency graph
requires: []
provides:
  - "All 4 main dashboard endpoints (executive, operations, financial, people-analytics) returning valid JSON without runtime errors"
  - "Fill-rate and coverage metrics counting ShiftAssignment records instead of non-existent Shift.assigned_employee_id"
  - "Financial cost metrics using ShiftAssignment.total_cost instead of non-existent Shift.cost"
  - "Attendance issue counts using ShiftAssignment.checked_in/check_in_time instead of missing Attendance model"
  - "PayrollSummary queries using period_start and gross_pay (correct columns)"
affects: [reporting, financial-dashboard, operations-dashboard, people-analytics]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fill-rate pattern: count distinct shift_ids via ShiftAssignment join with status != 'cancelled'"
    - "Attendance pattern: ShiftAssignment.checked_in + check_in_time for no-show/late arrival detection"
    - "Cost pattern: ShiftAssignment.total_cost, regular_pay, overtime_pay for all financial aggregations"

key-files:
  created: []
  modified:
    - backend/app/api/endpoints/dashboards.py

key-decisions:
  - "PayrollSummary has no org_id-per-employee filter issue: PayrollSummary.org_id exists directly on the model for executive/financial dashboards; stats endpoint already used Employee join correctly"
  - "Site.is_active does not exist: replaced all active_sites queries with simple count of all sites per org"
  - "No Attendance model: ShiftAssignment.checked_in/check_in_time fields replace all attendance tracking queries"
  - "distinct and extract imported at module level (not inline) for cleaner code"

patterns-established:
  - "ShiftAssignment fill-rate: use distinct(ShiftAssignment.shift_id) join filtered by status != 'cancelled'"
  - "PayrollSummary field names: period_start (not pay_period_start), gross_pay (not total_pay)"

# Metrics
duration: 8min
completed: 2026-03-03
---

# Phase 01.1 Plan 01: Dashboard Field Reference Fixes Summary

**All 4 dashboard endpoints (executive, operations, financial, people-analytics) corrected to reference actual SQLAlchemy model columns — eliminating NameError/AttributeError crashes on every call**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-03T09:24:41Z
- **Completed:** 2026-03-03T09:32:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Fixed 7 categories of broken model-field references that caused every dashboard call to crash
- Replaced all `Shift.assigned_employee_id` fill-rate patterns with `ShiftAssignment` join queries across all 4 dashboards
- Eliminated all references to non-existent `Attendance` model (replaced with `ShiftAssignment.checked_in`/`check_in_time`)
- Corrected `PayrollSummary` field names (`pay_period_start` → `period_start`, `total_pay` → `gross_pay`, removed `regular_pay`/`overtime_pay`)
- Replaced `Shift.cost` with `ShiftAssignment.total_cost` throughout financial dashboard
- Removed `Site.is_active` (column does not exist) and `Site.name` (correct field is `site_name`)

## Task Commits

1. **Task 1: Fix executive and operations dashboard field errors** - (combined with Task 2)
2. **Task 2: Fix financial and people-analytics dashboard field errors** - `fc77b7f` (fix)

## Files Created/Modified

- `backend/app/api/endpoints/dashboards.py` - Corrected all 4 dashboard endpoint functions: executive, operations, financial, people-analytics

## Decisions Made

- Combined Tasks 1 and 2 into a single commit since both modify the same file and corrections are tightly related
- `Site.is_active` removed entirely — active sites count now returns all sites for the org (no status filter exists on Site model)
- `PayrollSummary.regular_pay`/`overtime_pay` do not exist on the model — financial breakdown now derives regular/overtime from `ShiftAssignment.regular_pay`/`ShiftAssignment.overtime_pay` joined through `Shift`
- `distinct` and `extract` moved to top-level import (from sqlalchemy) alongside `func`, `and_`, `or_`, `case`

## Deviations from Plan

None - plan executed exactly as written. All corrections followed the plan's prescribed query patterns.

## Issues Encountered

None - all field corrections were straightforward substitutions.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 4 dashboard endpoints now return valid JSON on every call
- Fill-rate, cost, attendance, and payroll metrics all reference real DB columns
- Ready to proceed with next plan in phase 01.1-roster-downstream-integrity

---
*Phase: 01.1-roster-downstream-integrity*
*Completed: 2026-03-03*
