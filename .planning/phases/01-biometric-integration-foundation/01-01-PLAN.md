---
phase: 01-biometric-integration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/biometric.py
  - backend/app/models/attendance.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/019_add_biometric_attendance_models.py
  - backend/app/attendance/geofence.py
  - backend/app/attendance/__init__.py
autonomous: true

must_haves:
  truths:
    - "BiometricTemplate model stores encrypted face embeddings per employee"
    - "AttendanceRecord model captures timestamp, confidence, GPS, device type, verification method"
    - "SiteGeofence model defines GPS center + radius per site"
    - "Haversine geofence validation correctly determines inside/outside"
  artifacts:
    - path: "backend/app/models/biometric.py"
      provides: "BiometricTemplate, EnrollmentSession, VerificationAttempt models"
      contains: "class BiometricTemplate"
    - path: "backend/app/models/attendance.py"
      provides: "AttendanceRecord, SiteGeofence models"
      contains: "class AttendanceRecord"
    - path: "backend/app/attendance/geofence.py"
      provides: "Haversine distance + geofence validation"
      exports: ["validate_geofence", "haversine_distance"]
  key_links:
    - from: "backend/app/models/biometric.py"
      to: "backend/app/models/employee.py"
      via: "ForeignKey employee_id"
      pattern: "ForeignKey.*employees.employee_id"
    - from: "backend/app/models/attendance.py"
      to: "backend/app/models/site.py"
      via: "ForeignKey site_id"
      pattern: "ForeignKey.*sites.site_id"
---

<objective>
Create the database foundation for biometric authentication and attendance tracking. This includes all SQLAlchemy models (BiometricTemplate, EnrollmentSession, VerificationAttempt, AttendanceRecord, SiteGeofence), the Alembic migration, and the geofence validation utility.

Purpose: Every subsequent plan depends on these models. Without the data layer, enrollment, verification, and clock-in services cannot be built.
Output: 5 new database tables via Alembic migration, geofence validation utility with Haversine formula.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-biometric-integration-foundation/01-RESEARCH.md
@.planning/phases/01-biometric-integration-foundation/01-CONTEXT.md
@backend/app/models/__init__.py
@backend/app/models/employee.py
@backend/app/models/site.py
@backend/app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create biometric and attendance SQLAlchemy models</name>
  <files>
    backend/app/models/biometric.py
    backend/app/models/attendance.py
    backend/app/models/__init__.py
  </files>
  <action>
Create `backend/app/models/biometric.py` with these models following existing project patterns (Integer PKs, org_id FK for multi-tenancy, SQLAlchemy Column definitions):

**BiometricTemplate:**
- `template_id` (Integer PK)
- `org_id` (Integer FK to organizations.org_id, CASCADE, NOT NULL, indexed)
- `employee_id` (Integer FK to employees.employee_id, CASCADE, NOT NULL, indexed)
- `template_type` (String(50), NOT NULL) - values: "facial_facenet512", "fingerprint_webauthn"
- `encrypted_template` (LargeBinary, NOT NULL) - pgcrypto-encrypted face embedding (512-d vector as JSON)
- `embedding_dimensions` (Integer, NOT NULL) - e.g., 512 for FaceNet512
- `quality_score` (Numeric(5,2), nullable) - enrollment capture quality 0-100
- `enrolled_at` (DateTime with timezone, server_default=func.now())
- `enrolled_by` (Integer FK to users.user_id, SET NULL, nullable) - HR user who performed enrollment
- `is_active` (Boolean, default=True) - soft-disable without deletion
- UNIQUE constraint on (employee_id, template_type) - one template per type per employee
- Relationship: `employee = relationship("Employee")`

**EnrollmentSession:**
- `session_id` (Integer PK)
- `org_id` (Integer FK)
- `employee_id` (Integer FK to employees.employee_id)
- `initiated_by` (Integer FK to users.user_id) - HR user
- `status` (String(20)) - "pending", "in_progress", "completed", "failed"
- `template_type` (String(50))
- `attempts` (Integer, default=0)
- `last_attempt_at` (DateTime, nullable)
- `quality_feedback` (Text, nullable) - JSON string of quality check results
- `grace_period_end` (Date, nullable) - when manual clock-ins stop being allowed
- `created_at`, `updated_at` (DateTime with timezone)

**VerificationAttempt:**
- `attempt_id` (Integer PK)
- `org_id` (Integer FK)
- `employee_id` (Integer FK to employees.employee_id, indexed)
- `template_type` (String(50))
- `confidence` (Numeric(5,2), NOT NULL) - 0-100 percentage
- `threshold_used` (Numeric(5,2), NOT NULL) - what threshold was applied
- `status` (String(20)) - "success", "success_with_warning", "failed"
- `requires_hr_review` (Boolean, default=False)
- `gps_lat` (Float, nullable)
- `gps_lng` (Float, nullable)
- `gps_accuracy` (Float, nullable) - meters
- `device_type` (String(50), nullable) - "phone", "desktop", "hardware_terminal"
- `created_at` (DateTime with timezone, server_default=func.now())
- Index on (employee_id, created_at) for adaptive threshold queries

Create `backend/app/models/attendance.py` with:

**AttendanceRecord:**
- `record_id` (Integer PK)
- `org_id` (Integer FK, NOT NULL, indexed)
- `employee_id` (Integer FK to employees.employee_id, NOT NULL, indexed)
- `site_id` (Integer FK to sites.site_id, SET NULL, nullable, indexed)
- `clock_in_at` (DateTime with timezone, NOT NULL)
- `clock_out_at` (DateTime with timezone, nullable)
- `verification_method` (String(30), NOT NULL) - "biometric_face", "biometric_fingerprint", "gps_only", "manual_hr_approved"
- `biometric_confidence` (Numeric(5,2), nullable) - 0-100
- `gps_lat` (Float, nullable)
- `gps_lng` (Float, nullable)
- `gps_accuracy` (Float, nullable)
- `gps_validated` (Boolean, default=False)
- `device_type` (String(50), nullable)
- `requires_hr_review` (Boolean, default=False)
- `hr_reviewed_by` (Integer FK to users.user_id, nullable)
- `hr_reviewed_at` (DateTime, nullable)
- `notes` (Text, nullable) - JSON array of log notes
- `created_at`, `updated_at` (DateTime with timezone)
- Relationships: employee, site, reviewer

**SiteGeofence:**
- `geofence_id` (Integer PK)
- `org_id` (Integer FK, NOT NULL, indexed)
- `site_id` (Integer FK to sites.site_id, CASCADE, NOT NULL, UNIQUE)
- `center_lat` (Float, NOT NULL)
- `center_lng` (Float, NOT NULL)
- `radius_meters` (Integer, NOT NULL, default=200) - research recommends 100m site + 2x50m GPS error buffer
- `is_active` (Boolean, default=True)
- `created_at`, `updated_at` (DateTime with timezone)
- Relationship: site

Update `backend/app/models/__init__.py` to import all new models: BiometricTemplate, EnrollmentSession, VerificationAttempt, AttendanceRecord, SiteGeofence. Add them to __all__.

Use the same patterns as existing models: `from app.database import Base`, SQLAlchemy Column/relationship imports, enum classes as needed (use string enums for status fields rather than SQLAlchemy Enum to avoid migration complexity).
  </action>
  <verify>
Run `python -c "from app.models import BiometricTemplate, EnrollmentSession, VerificationAttempt, AttendanceRecord, SiteGeofence; print('All models imported successfully')"` from the backend directory.
  </verify>
  <done>All 5 models importable from app.models, each with correct column types, foreign keys to employees/sites/users/organizations, and UNIQUE constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration and geofence validation utility</name>
  <files>
    backend/alembic/versions/019_add_biometric_attendance_models.py
    backend/app/attendance/__init__.py
    backend/app/attendance/geofence.py
  </files>
  <action>
**Alembic Migration:**
Generate migration file `backend/alembic/versions/019_add_biometric_attendance_models.py` (check current highest number and use next). The migration should:
1. Enable pgcrypto extension: `op.execute("CREATE EXTENSION IF NOT EXISTS pgcrypto")`
2. Create `biometric_templates` table with all columns from BiometricTemplate model
3. Create `enrollment_sessions` table
4. Create `verification_attempts` table with composite index on (employee_id, created_at)
5. Create `attendance_records` table
6. Create `site_geofences` table with UNIQUE on site_id
7. Downgrade: Drop all 5 tables in reverse order

Use `sa.Column`, `sa.ForeignKey`, `sa.Index` patterns matching existing migrations. Use `op.create_table()` for each.

**Geofence Utility:**
Create `backend/app/attendance/__init__.py` (empty).
Create `backend/app/attendance/geofence.py` with:

```python
def haversine_distance(lat1: float, lon1: float, lat2: float, lon2: float) -> float:
    """Calculate distance between two GPS points in meters using Haversine formula."""
    # Earth radius 6371e3 meters
    # Convert to radians, apply formula
    # Return distance in meters

def validate_geofence(
    employee_lat: float,
    employee_lng: float,
    gps_accuracy: float,
    center_lat: float,
    center_lng: float,
    radius_meters: int
) -> dict:
    """
    Validate if employee GPS position is within site geofence.

    Returns dict with:
    - inside: bool (within radius + accuracy buffer)
    - distance: float (meters from center)
    - accuracy: float (GPS accuracy in meters)
    - effective_radius: float (radius + accuracy buffer)

    Raises ValueError if gps_accuracy > 50 (too imprecise per research).
    """
```

Do NOT use external libraries for Haversine - it's a simple formula (research shows geopy is overkill for this). Use math.radians, math.sin, math.cos, math.sqrt, math.atan2.

Graduated validation logic:
- distance <= radius: "inside" (auto-approve)
- distance <= radius + accuracy: "inside" (GPS uncertainty accounts for it)
- distance > radius + accuracy: "outside"
- gps_accuracy > 50: raise ValueError (too imprecise, per research pitfall #3)
  </action>
  <verify>
1. Run `cd backend && alembic upgrade head` - migration applies without errors
2. Run `python -c "from app.attendance.geofence import validate_geofence, haversine_distance; print(haversine_distance(-26.2041, 28.0473, -26.2051, 28.0483))"` - returns a distance in meters (roughly 130m for these Johannesburg coords)
3. Run `python -c "from app.attendance.geofence import validate_geofence; r = validate_geofence(-26.2041, 28.0473, 10.0, -26.2041, 28.0473, 200); print(r)"` - returns inside=True, distance~0
  </verify>
  <done>Alembic migration creates all 5 tables with pgcrypto extension. Geofence utility correctly calculates Haversine distance and validates GPS positions against circular geofences with accuracy buffers.</done>
</task>

</tasks>

<verification>
1. All 5 new models importable from `app.models`
2. `alembic upgrade head` succeeds (tables created in PostgreSQL)
3. `alembic downgrade -1` succeeds (tables dropped cleanly)
4. Geofence `haversine_distance` returns correct meters between known GPS coordinates
5. Geofence `validate_geofence` correctly handles inside/outside/accuracy-too-low cases
</verification>

<success_criteria>
- 5 database tables exist: biometric_templates, enrollment_sessions, verification_attempts, attendance_records, site_geofences
- pgcrypto extension enabled in PostgreSQL
- Geofence utility validates GPS coordinates against circular geofences with accuracy buffering
- All foreign keys correctly reference employees, sites, users, organizations tables
- Multi-tenancy enforced via org_id on all tables
</success_criteria>

<output>
After completion, create `.planning/phases/01-biometric-integration-foundation/01-01-SUMMARY.md`
</output>
