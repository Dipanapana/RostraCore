---
phase: 00.2-localization-multi-country-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/request.ts
  - frontend/src/i18n/config.ts
  - frontend/src/messages/en.json
  - frontend/src/messages/af.json
  - frontend/src/messages/zu.json
  - frontend/next.config.js
  - frontend/src/app/layout.tsx
  - frontend/src/components/LanguageSelector.tsx
  - frontend/package.json
autonomous: true

must_haves:
  truths:
    - "next-intl is installed and configured in next.config.js"
    - "Layout wraps children with NextIntlClientProvider"
    - "English, Afrikaans, and Zulu translation files exist"
    - "useTranslations hook works in client components"
    - "Language selector component switches locale immediately"
    - "Locale persists via cookie across page refreshes"
  artifacts:
    - path: "frontend/src/i18n/request.ts"
      provides: "next-intl request configuration (locale from cookie)"
      contains: "getRequestConfig"
    - path: "frontend/src/i18n/config.ts"
      provides: "Supported locales definition"
      exports: ["SUPPORTED_LOCALES", "DEFAULT_LOCALE"]
    - path: "frontend/src/messages/en.json"
      provides: "English translation messages"
      min_lines: 50
    - path: "frontend/src/messages/af.json"
      provides: "Afrikaans translation messages"
      min_lines: 50
    - path: "frontend/src/messages/zu.json"
      provides: "Zulu translation messages"
      min_lines: 50
    - path: "frontend/src/components/LanguageSelector.tsx"
      provides: "Language switching dropdown component"
      contains: "LanguageSelector"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/i18n/request.ts"
      via: "NextIntlClientProvider with getMessages"
      pattern: "NextIntlClientProvider"
    - from: "frontend/src/i18n/request.ts"
      to: "frontend/src/messages/*.json"
      via: "Dynamic import of locale messages"
      pattern: "import.*messages"
    - from: "frontend/src/components/LanguageSelector.tsx"
      to: "cookie"
      via: "document.cookie locale setting"
      pattern: "cookie|locale"
---

<objective>
Set up the next-intl internationalization framework for the frontend using the "without routing" pattern (locale from user preferences/cookie, no URL changes). Create English, Afrikaans, and Zulu translation files covering all existing UI text. Build a language selector component that immediately switches the UI language.

Purpose: Users must be able to use RostraCore in their preferred language. South Africa has 11 official languages; English, Afrikaans, and Zulu are the most widely used in the workforce management context. The i18n framework also enables future expansion to French, Portuguese, Swahili for other African markets.

Output: Fully functional i18n with 3 languages, language selector component, next-intl configured.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.2-localization-multi-country-compliance/00.2-RESEARCH.md
@.planning/phases/00.2-localization-multi-country-compliance/00.2-CONTEXT.md

Key existing files:
@frontend/next.config.js - Wrap with createNextIntlPlugin
@frontend/src/app/layout.tsx - Wrap with NextIntlClientProvider
@frontend/package.json - Install next-intl

Note: The app uses static export mode for Tauri desktop builds (BUILD_MODE=desktop).
next-intl's "without routing" mode is compatible with static export because it uses
client-side locale detection (cookie/localStorage), not server-side routing.
For the desktop build, locale detection should fall back to localStorage if cookies
are not available in the Tauri webview.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-intl and configure infrastructure</name>
  <files>
    frontend/package.json
    frontend/next.config.js
    frontend/src/i18n/request.ts
    frontend/src/i18n/config.ts
    frontend/src/app/layout.tsx
  </files>
  <action>
    1. Install next-intl:
    ```bash
    cd frontend && npm install next-intl
    ```

    2. Create `frontend/src/i18n/config.ts`:
    ```typescript
    export const SUPPORTED_LOCALES = ['en', 'af', 'zu'] as const;
    export type SupportedLocale = typeof SUPPORTED_LOCALES[number];
    export const DEFAULT_LOCALE: SupportedLocale = 'en';

    export const LOCALE_NAMES: Record<SupportedLocale, string> = {
      en: 'English',
      af: 'Afrikaans',
      zu: 'isiZulu',
    };
    ```

    3. Create `frontend/src/i18n/request.ts` using next-intl's "without routing" pattern:
    ```typescript
    import { getRequestConfig } from 'next-intl/server';
    import { cookies } from 'next/headers';
    import { SUPPORTED_LOCALES, DEFAULT_LOCALE } from './config';
    import type { SupportedLocale } from './config';

    export default getRequestConfig(async () => {
      // Read locale from cookie (set by LanguageSelector)
      let locale: SupportedLocale = DEFAULT_LOCALE;

      try {
        const store = await cookies();
        const rawLocale = store.get('locale')?.value;
        if (rawLocale && SUPPORTED_LOCALES.includes(rawLocale as SupportedLocale)) {
          locale = rawLocale as SupportedLocale;
        }
      } catch {
        // Cookie reading may fail in static export / Tauri desktop
        // Fall back to default locale
      }

      return {
        locale,
        messages: (await import(`../../messages/${locale}.json`)).default,
      };
    });
    ```

    4. Update `frontend/next.config.js`:
    - Add `const createNextIntlPlugin = require('next-intl/plugin');` at top
    - Add `const withNextIntl = createNextIntlPlugin();` after the plugin import
    - Change the final export to wrap with `withNextIntl`:
      - Where it currently does `module.exports = withSentryConfig(nextConfig, ...)` change to `module.exports = withSentryConfig(withNextIntl(nextConfig), ...)`
      - Where it currently does `module.exports = nextConfig` change to `module.exports = withNextIntl(nextConfig)`

    **Important:** The next-intl plugin needs to wrap the config BEFORE Sentry. Keep the existing Sentry conditional logic intact. The plugin call `createNextIntlPlugin()` with no arguments will auto-discover `src/i18n/request.ts`.

    5. Update `frontend/src/app/layout.tsx`:
    - This file currently uses `export const metadata` which means it's a Server Component (good, required by next-intl)
    - Convert to async function to use `getLocale()` and `getMessages()`
    - Add imports: `import { NextIntlClientProvider } from 'next-intl'` and `import { getLocale, getMessages } from 'next-intl/server'`
    - Make RootLayout async: `export default async function RootLayout(...)`
    - Get locale and messages: `const locale = await getLocale(); const messages = await getMessages();`
    - Change `<html lang="en">` to `<html lang={locale}>`
    - Wrap the body content with `<NextIntlClientProvider messages={messages}>...</NextIntlClientProvider>` INSIDE the existing `<QueryProvider>` wrapper (NextIntlClientProvider should be the outermost provider after QueryProvider, before ErrorBoundary)

    **Critical consideration for desktop static export:** When `BUILD_MODE=desktop`, Next.js uses static export which doesn't support Server Components' `cookies()` API at runtime. The `request.ts` try/catch handles this gracefully. The `NextIntlClientProvider` will receive the default locale messages at build time. For runtime locale switching in Tauri, the `LanguageSelector` component (Task 2) will use `localStorage` as fallback and trigger a page reload.
  </action>
  <verify>
    - Run `cd frontend && npm ls next-intl` to confirm installation
    - Run `cd frontend && npx next build 2>&1 | head -20` to verify build doesn't break (or at minimum check that the config file syntax is valid)
    - Verify `frontend/src/i18n/request.ts` exists
    - Verify `frontend/src/i18n/config.ts` exports SUPPORTED_LOCALES
    - Verify `frontend/src/app/layout.tsx` contains NextIntlClientProvider
  </verify>
  <done>
    next-intl installed and configured. Layout wraps with NextIntlClientProvider. Locale read from cookie with fallback to 'en'. Config defines 3 supported locales.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create translation files and language selector component</name>
  <files>
    frontend/src/messages/en.json
    frontend/src/messages/af.json
    frontend/src/messages/zu.json
    frontend/src/components/LanguageSelector.tsx
  </files>
  <action>
    1. Create `frontend/src/messages/en.json` with translation keys for ALL existing UI text. Organize by namespace matching the page/component structure:

    ```json
    {
      "common": {
        "loading": "Loading...",
        "error": "An error occurred",
        "save": "Save",
        "cancel": "Cancel",
        "delete": "Delete",
        "edit": "Edit",
        "add": "Add",
        "search": "Search",
        "filter": "Filter",
        "export": "Export",
        "import": "Import",
        "confirm": "Confirm",
        "back": "Back",
        "next": "Next",
        "submit": "Submit",
        "close": "Close",
        "yes": "Yes",
        "no": "No",
        "actions": "Actions",
        "status": "Status",
        "active": "Active",
        "inactive": "Inactive",
        "noResults": "No results found",
        "required": "Required"
      },
      "nav": {
        "dashboard": "Dashboard",
        "employees": "Employees",
        "roster": "Roster",
        "shifts": "Shifts",
        "payroll": "Payroll",
        "clients": "Clients",
        "sites": "Sites",
        "settings": "Settings",
        "billing": "Billing",
        "reports": "Reports",
        "logout": "Log Out",
        "profile": "Profile"
      },
      "auth": {
        "login": "Log In",
        "signup": "Sign Up",
        "email": "Email",
        "password": "Password",
        "forgotPassword": "Forgot Password?",
        "resetPassword": "Reset Password",
        "rememberMe": "Remember Me",
        "loginError": "Invalid email or password",
        "sessionExpired": "Your session has expired. Please log in again."
      },
      "dashboard": {
        "title": "Dashboard",
        "welcome": "Welcome back",
        "totalEmployees": "Total Employees",
        "activeShifts": "Active Shifts",
        "attendanceRate": "Attendance Rate",
        "upcomingShifts": "Upcoming Shifts",
        "recentActivity": "Recent Activity",
        "complianceStatus": "Compliance Status",
        "quickActions": "Quick Actions"
      },
      "employees": {
        "title": "Employees",
        "addEmployee": "Add Employee",
        "editEmployee": "Edit Employee",
        "firstName": "First Name",
        "lastName": "Last Name",
        "idNumber": "ID Number",
        "phoneNumber": "Phone Number",
        "email": "Email Address",
        "employeeCode": "Employee Code",
        "role": "Role",
        "employmentType": "Employment Type",
        "hireDate": "Hire Date",
        "hourlyRate": "Hourly Rate",
        "status": "Status",
        "totalEmployees": "Total Employees",
        "permanent": "Permanent",
        "contract": "Contract",
        "partTime": "Part Time",
        "temporary": "Temporary",
        "consultant": "Consultant"
      },
      "roster": {
        "title": "Roster",
        "generateRoster": "Generate Roster",
        "publishRoster": "Publish Roster",
        "unfilledShifts": "Unfilled Shifts",
        "weekView": "Week View",
        "monthView": "Month View",
        "shiftAssignment": "Shift Assignment",
        "assignEmployee": "Assign Employee",
        "removeAssignment": "Remove Assignment",
        "conflicts": "Conflicts",
        "complianceCheck": "Compliance Check"
      },
      "payroll": {
        "title": "Payroll",
        "generatePayroll": "Generate Payroll",
        "payrollPeriod": "Payroll Period",
        "grossPay": "Gross Pay",
        "netPay": "Net Pay",
        "deductions": "Deductions",
        "totalHours": "Total Hours",
        "overtimeHours": "Overtime Hours",
        "paye": "PAYE",
        "uif": "UIF",
        "sdl": "SDL",
        "totalDeductions": "Total Deductions",
        "costToCompany": "Cost to Company",
        "downloadPayslip": "Download Payslip",
        "exportPayroll": "Export Payroll"
      },
      "settings": {
        "title": "Settings",
        "general": "General",
        "language": "Language",
        "languageDescription": "Choose your preferred language",
        "currency": "Currency",
        "country": "Country",
        "notifications": "Notifications",
        "security": "Security",
        "organization": "Organization",
        "shiftPatterns": "Shift Patterns"
      },
      "offline": {
        "banner": "You are currently offline",
        "syncPending": "Changes pending sync",
        "syncNow": "Sync Now",
        "syncing": "Syncing...",
        "syncComplete": "Sync complete",
        "syncFailed": "Sync failed",
        "lastSynced": "Last synced",
        "offlineMode": "Offline Mode",
        "onlineMode": "Online"
      },
      "compliance": {
        "maxWeeklyHours": "Maximum Weekly Hours",
        "minRestHours": "Minimum Rest Between Shifts",
        "maxConsecutiveDays": "Maximum Consecutive Days",
        "overtimeThreshold": "Overtime Threshold",
        "violations": "Violations",
        "warnings": "Warnings",
        "compliant": "Compliant",
        "nonCompliant": "Non-Compliant"
      }
    }
    ```

    2. Create `frontend/src/messages/af.json` with Afrikaans translations for ALL the same keys. Use proper Afrikaans (not Google Translate quality). Key translations:
    - Dashboard = Kontroleskerm
    - Employees = Werknemers
    - Roster = Rooster
    - Payroll = Betaalstaat
    - Settings = Instellings
    - Loading = Laai...
    - Save = Stoor
    - Cancel = Kanselleer
    - Delete = Verwyder
    - Search = Soek
    - Active = Aktief
    - Inactive = Onaktief
    - Log In = Meld Aan
    - Log Out = Meld Af
    - Shifts = Skofte
    - Gross Pay = Bruto Betaling
    - Net Pay = Netto Betaling
    - Deductions = Aftrekkings
    - Overtime = Oortyd
    - Compliance = Nakoming
    - Offline = Aflyn
    - Online = Aanlyn

    3. Create `frontend/src/messages/zu.json` with isiZulu translations. Key translations:
    - Dashboard = Ibhodi Elilawulayo
    - Employees = Abasebenzi
    - Roster = Uhlu Lwemisebenzi
    - Payroll = Uhlu Lwamaholo
    - Settings = Izilungiselelo
    - Loading = Iyalayisha...
    - Save = Gcina
    - Cancel = Khansela
    - Delete = Susa
    - Search = Sesha
    - Active = Kusebenza
    - Inactive = Akusebenzi
    - Log In = Ngena
    - Log Out = Phuma
    - Shifts = Amashifu
    - Gross Pay = Iholo Eliphelele
    - Net Pay = Iholo Elisele
    - Deductions = Izaphulelo
    - Overtime = Isikhathi Esengeziwe
    - Compliance = Ukuthobela

    4. Create `frontend/src/components/LanguageSelector.tsx`:
    ```tsx
    'use client';

    import { useState, useRef, useEffect } from 'react';
    import { useLocale } from 'next-intl';
    import { useRouter } from 'next/navigation';
    import { SUPPORTED_LOCALES, LOCALE_NAMES } from '@/i18n/config';
    import type { SupportedLocale } from '@/i18n/config';
    import { Globe } from 'lucide-react';

    export function LanguageSelector() {
      const locale = useLocale() as SupportedLocale;
      const router = useRouter();
      const [isOpen, setIsOpen] = useState(false);
      const dropdownRef = useRef<HTMLDivElement>(null);

      // Close dropdown on outside click
      useEffect(() => {
        function handleClickOutside(event: MouseEvent) {
          if (dropdownRef.current && !dropdownRef.current.contains(event.target as Node)) {
            setIsOpen(false);
          }
        }
        document.addEventListener('mousedown', handleClickOutside);
        return () => document.removeEventListener('mousedown', handleClickOutside);
      }, []);

      function handleLocaleChange(newLocale: SupportedLocale) {
        // Set cookie for SSR locale detection
        document.cookie = `locale=${newLocale};path=/;max-age=${365 * 24 * 60 * 60}`;

        // Also store in localStorage for Tauri desktop fallback
        try {
          localStorage.setItem('locale', newLocale);
        } catch {}

        setIsOpen(false);

        // Refresh the page to apply the new locale
        router.refresh();
      }

      return (
        <div ref={dropdownRef} className="relative">
          <button
            onClick={() => setIsOpen(!isOpen)}
            className="flex items-center gap-2 px-3 py-2 text-sm rounded-lg
                       hover:bg-gray-100 dark:hover:bg-gray-700
                       text-gray-700 dark:text-gray-300
                       transition-colors"
            aria-label="Select language"
          >
            <Globe className="w-4 h-4" />
            <span>{LOCALE_NAMES[locale]}</span>
          </button>

          {isOpen && (
            <div className="absolute right-0 mt-1 w-40 rounded-lg border
                            border-gray-200 dark:border-gray-600
                            bg-white dark:bg-gray-800
                            shadow-lg z-50">
              {SUPPORTED_LOCALES.map((loc) => (
                <button
                  key={loc}
                  onClick={() => handleLocaleChange(loc)}
                  className={`w-full text-left px-4 py-2 text-sm
                    hover:bg-gray-100 dark:hover:bg-gray-700
                    first:rounded-t-lg last:rounded-b-lg
                    ${loc === locale
                      ? 'bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 font-medium'
                      : 'text-gray-700 dark:text-gray-300'
                    }`}
                >
                  {LOCALE_NAMES[loc]}
                </button>
              ))}
            </div>
          )}
        </div>
      );
    }
    ```

    **Important:** The LanguageSelector uses `router.refresh()` to re-render with new locale. This avoids a full page reload while still picking up the new cookie value. For the Tauri desktop build where cookies may not work, the component also stores locale in localStorage.

    **Translation quality:** Write proper, natural translations. Avoid robotic machine-translated text. For Afrikaans and Zulu, use workforce/business terminology that South African workers would recognize. Where a direct translation sounds awkward, use the commonly understood English term (e.g., "PAYE" stays as "PAYE" in all languages since it's a standard acronym).
  </action>
  <verify>
    - Verify all 3 JSON files are valid JSON: `cd frontend && node -e "require('./src/messages/en.json'); require('./src/messages/af.json'); require('./src/messages/zu.json'); console.log('All message files valid')"`
    - Verify all 3 files have the same key structure: `cd frontend && node -e "const en = require('./src/messages/en.json'); const af = require('./src/messages/af.json'); const zu = require('./src/messages/zu.json'); const enKeys = JSON.stringify(Object.keys(en).sort()); const afKeys = JSON.stringify(Object.keys(af).sort()); const zuKeys = JSON.stringify(Object.keys(zu).sort()); console.log('Keys match:', enKeys === afKeys && afKeys === zuKeys)"`
    - Verify LanguageSelector.tsx exists and has no TypeScript compile errors
  </verify>
  <done>
    3 complete translation files (en, af, zu) with matching key structures. LanguageSelector component switches locale immediately via cookie + localStorage. All translations use natural, workforce-appropriate language.
  </done>
</task>

</tasks>

<verification>
1. `npm ls next-intl` shows next-intl installed
2. `next.config.js` wraps config with `withNextIntl`
3. `layout.tsx` has `NextIntlClientProvider` and async function
4. All 3 message files valid JSON with matching key structures
5. `LanguageSelector.tsx` exists and exports component
6. `request.ts` reads locale from cookie with fallback
</verification>

<success_criteria>
- next-intl installed and configured with "without routing" pattern
- Layout wraps all content with NextIntlClientProvider
- English, Afrikaans, and Zulu translation files exist with 80+ keys each
- LanguageSelector component lets users switch language immediately
- Locale persisted via cookie (web) and localStorage (Tauri desktop fallback)
- useTranslations() hook works in client components
- Build completes successfully with next-intl configured
</success_criteria>

<output>
After completion, create `.planning/phases/00.2-localization-multi-country-compliance/00.2-02-SUMMARY.md`
</output>
