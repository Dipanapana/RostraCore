---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/endpoints/settings.py
  - backend/app/api/endpoints/payroll_deductions.py
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "A request to GET /api/v1/settings/constraints without a JWT returns 401, not constraint data"
    - "A request to PUT /api/v1/settings/constraints without a JWT returns 401, not a successful update"
    - "A request to POST /api/v1/settings/constraints/reset without a JWT returns 401"
    - "A request to GET /api/v1/payroll-deductions/paye without a JWT returns 401"
    - "A request to GET /api/v1/payroll-deductions/tax-tables without a JWT returns 401"
    - "Authenticated admin users can still call these endpoints successfully"
  artifacts:
    - path: "backend/app/api/endpoints/settings.py"
      provides: "Protected constraint settings endpoints"
      contains: "get_current_user"
    - path: "backend/app/api/endpoints/payroll_deductions.py"
      provides: "Protected tax calculation endpoints"
      contains: "get_current_user"
  key_links:
    - from: "backend/app/api/endpoints/settings.py"
      to: "app.auth.security.get_current_user"
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
    - from: "backend/app/api/endpoints/payroll_deductions.py"
      to: "app.auth.security.get_current_user"
      via: "Depends(get_current_user) on all unprotected endpoints"
      pattern: "Depends\\(get_current_user\\)"
---

<objective>
Protect the two endpoint files that currently have no authentication — settings.py and the unauthenticated routes in payroll_deductions.py.

Purpose: These endpoints currently leak internal configuration (solver constraints, BCEA tax tables, payroll calculation logic) to any unauthenticated HTTP request. This is a direct SEC-01 violation.

Output: Both files updated so every route requires a valid JWT. Unauthenticated requests return 401.
</objective>

<execution_context>
@C:/Users/USER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/api/endpoints/settings.py
@backend/app/api/endpoints/payroll_deductions.py
@backend/app/auth/security.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth to settings.py constraint endpoints</name>
  <files>backend/app/api/endpoints/settings.py</files>
  <action>
    The file currently has 3 unprotected endpoints: GET /constraints, PUT /constraints, POST /constraints/reset. All three expose and modify the solver's runtime configuration without any authentication.

    Add authentication to all three:

    1. Add import at top of file: `from app.auth.security import get_current_user, is_admin`
    2. Add `from fastapi import APIRouter, HTTPException, Depends` (update existing import to include Depends)
    3. Add `from app.models.user import User` import
    4. For GET /constraints: add `current_user: User = Depends(get_current_user)` parameter — any authenticated user can read constraints
    5. For PUT /constraints: add `current_user: User = Depends(is_admin)` parameter — only admins should modify runtime constraints
    6. For POST /constraints/reset: add `current_user: User = Depends(is_admin)` parameter — only admins should reset constraints

    Keep all existing logic unchanged. Only add the dependency parameters and necessary imports.

    Note: `is_admin` is a FastAPI dependency function (not a regular function) — use `Depends(is_admin)`, not `Depends(is_admin(current_user))`.
  </action>
  <verify>
    Start the backend: `cd backend && python -m uvicorn app.main:app --port 8001`

    Test unauthenticated access (should return 401):
    ```
    curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/api/v1/settings/constraints
    ```
    Expected: 401

    Test PUT without auth (should return 401):
    ```
    curl -s -o /dev/null -w "%{http_code}" -X PUT http://localhost:8001/api/v1/settings/constraints -H "Content-Type: application/json" -d '{}'
    ```
    Expected: 401 or 422 (422 means FastAPI validated body before auth, which is acceptable)

    Verify the file imports compile without error:
    ```
    cd backend && python -c "from app.api.endpoints.settings import router; print('OK')"
    ```
    Expected: OK
  </verify>
  <done>
    GET /api/v1/settings/constraints returns 401 for unauthenticated requests.
    PUT /api/v1/settings/constraints returns 401 for unauthenticated requests.
    POST /api/v1/settings/constraints/reset returns 401 for unauthenticated requests.
    Import check passes with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add auth to unprotected payroll_deductions.py endpoints</name>
  <files>backend/app/api/endpoints/payroll_deductions.py</files>
  <action>
    The file currently has 6 unprotected endpoints (GET /paye, GET /uif, GET /sdl, GET /net-pay, GET /cost-to-company, GET /tax-tables). These expose BCEA tax calculation logic and business financial data without authentication. The endpoints GET /config and PUT /config are already protected via `require_finance_access`.

    Fix: Add `current_user: User = Depends(get_current_user)` to the 6 unprotected endpoints.

    Steps:
    1. Check current imports — `require_finance_access` is already imported from `app.auth.security`. Add `get_current_user` to that import line.
    2. Add `from app.models.user import User` if not already present.
    3. For each of these 6 endpoint functions, add `current_user: User = Depends(get_current_user)` as a parameter:
       - `async def calculate_paye(...)` — add as first param after any existing query params
       - `async def calculate_uif(...)`
       - `async def calculate_sdl(...)`
       - `async def calculate_net_pay(...)`
       - `async def calculate_cost_to_company(...)`
       - `async def get_tax_tables(...)` — this one has no params at all, add `current_user: User = Depends(get_current_user)` as sole param

    Do NOT change the /config endpoints — they already use `require_finance_access` which is stricter (finance role required).

    The `current_user` parameter does not need to be used in the function body — its presence as a `Depends` is sufficient to enforce auth.
  </action>
  <verify>
    Test unauthenticated tax table access (should return 401):
    ```
    curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/api/v1/payroll-deductions/tax-tables
    ```
    Expected: 401

    Test unauthenticated PAYE calculation (should return 401):
    ```
    curl -s -o /dev/null -w "%{http_code}" "http://localhost:8001/api/v1/payroll-deductions/paye?gross_salary=30000"
    ```
    Expected: 401

    Verify the module imports cleanly:
    ```
    cd backend && python -c "from app.api.endpoints.payroll_deductions import router; print('OK')"
    ```
    Expected: OK
  </verify>
  <done>
    All 6 previously unprotected payroll deduction endpoints return 401 for unauthenticated requests.
    The /config and /config PUT endpoints remain protected by require_finance_access (unchanged).
    Import check passes with no errors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Confirm import health across both files:
   ```
   cd backend && python -c "
   from app.api.endpoints.settings import router as s
   from app.api.endpoints.payroll_deductions import router as p
   print('Both imports OK')
   "
   ```

2. Run the existing test suite to ensure no regressions:
   ```
   cd backend && python -m pytest tests/ -x -q 2>&1 | tail -20
   ```

3. Check the full app starts without import errors:
   ```
   cd backend && python -c "from app.main import app; print('App loaded OK')"
   ```
</verification>

<success_criteria>
- settings.py: all 3 constraint endpoints require auth (401 without JWT)
- payroll_deductions.py: all 6 tax calculation endpoints require auth (401 without JWT)
- No test regressions from changes
- App still imports and starts cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-01-SUMMARY.md`
</output>
